/**
 * Desktop matrix helpers — re-exported from the shared `picly-ml` package.
 * (umeyama / svd2 are pure linear algebra, used by faceAnalysis + tools.)
 */
export { umeyama, svd2, det2, type Mat2 } from 'picly-ml'
